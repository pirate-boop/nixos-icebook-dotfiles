name: Validate Bootloader

on:
  workflow_call:

jobs:
  bootloader-safety:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      # ... (Сюда копируешь все шаги из bootloader-safety: конфликт, device, EFI и т.д.)
      # Я пропущу их для краткости, они остаются такими же
