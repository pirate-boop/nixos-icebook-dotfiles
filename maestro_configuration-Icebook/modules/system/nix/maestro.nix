#****************************************************************#
# |============================================================| #
# |SYSTEM — дирижёр системных модулей                          | #
# |============================================================| #
#****************************************************************#
{ config, pkgs, inputs, ... }: {
  imports = [
    ./nixpkgs.nix
    ./stateVersion.nix
    ./lix.nix
    ./binary-caches/maestro_binary_caches.nix
  ];
}
