# ~/.config/mango/config.conf

exec-once=dms run

# ============ LAYOUT (niri-like scrollable columns) ============
circle_layout=scroller,tile,monocle
new_is_master=1
default_mfact=0.55
default_nmaster=1
smartgaps=0

scroller_default_proportion=0.9
scroller_focus_center=1
scroller_prefer_overspread=0
scroller_proportion_preset=0.33,0.5,0.66

tagrule=id:1,layout_name:scroller
tagrule=id:2,layout_name:scroller
tagrule=id:3,layout_name:scroller
tagrule=id:4,layout_name:scroller
tagrule=id:5,layout_name:scroller

# ============ OVERVIEW (niri-like Overview) ============
enable_hotarea=1
hotarea_corner=0
hotarea_size=10
ov_tab_mode=1
overviewgappi=8
overviewgappo=30

# ============ APPS ============
bind=SUPER,z,spawn,AyuGram
bind=SUPER,w,spawn,vivaldi
bind=SUPER,t,spawn,wezterm
bind=SUPER,d,spawn,vesktop
bind=SUPER,e,spawn,thunar
bind=SUPER,m,spawn,pear-desktop

# ============ SESSION ============
bind=SUPER,Escape,killclient,
bind=SUPER+SHIFT,m,quit,
bind=SUPER,r,reload_config
bind=SUPER+SHIFT,l,spawn,dms ipc call lock lock
bind=SUPER,p,spawn,dms ipc call powermenu toggle

# ============ WINDOW STATE ============
bind=ALT,f,togglefullscreen,
bind=ALT+SHIFT,f,togglefakefullscreen,
bind=ALT,backslash,togglefloating,
bind=ALT,a,togglemaximizescreen,
bind=SUPER,g,toggleglobal,
bind=ALT,Tab,toggleoverview,
bind=SUPER,i,minimized,
bind=SUPER+SHIFT,i,restore_minimized,
bind=ALT,z,toggle_scratchpad,

# ============ FOCUS / MOVE (hjkl) ============
bind=SUPER,h,focusdir,left
bind=SUPER,l,focusdir,right
bind=SUPER,k,focusdir,up
bind=SUPER,j,focusdir,down
bind=SUPER,Tab,focusstack,next

bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,j,exchange_client,down

bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,view,-1
bind=SUPER,Down,view,+1

# ============ TAGS (workspaces) ============
bind=SUPER,1,view,1
bind=SUPER,2,view,2
bind=SUPER,3,view,3
bind=SUPER,4,view,4
bind=SUPER,5,view,5

bind=SUPER+SHIFT,1,tag,1
bind=SUPER+SHIFT,2,tag,2
bind=SUPER+SHIFT,3,tag,3
bind=SUPER+SHIFT,4,tag,4
bind=SUPER+SHIFT,5,tag,5

# ============ MOUSE ============
mousebind=SUPER+drag,movewin
mousebind=SUPER+SHIFT+drag,resizewin

# ============ DMS: AUDIO/MEDIA ============
bind=NONE,XF86AudioRaiseVolume,spawn,dms ipc call audio increment 3
bind=NONE,XF86AudioLowerVolume,spawn,dms ipc call audio decrement 3
bind=NONE,XF86AudioMute,spawn,dms ipc call audio mute
bind=NONE,XF86AudioPlay,spawn,dms ipc call mpris playPause
bind=NONE,XF86AudioNext,spawn,dms ipc call mpris next
bind=NONE,XF86AudioPrev,spawn,dms ipc call mpris previous

# ============ DMS: BRIGHTNESS ============
bind=NONE,XF86MonBrightnessUp,spawn,dms ipc call brightness increment 5
bind=NONE,XF86MonBrightnessDown,spawn,dms ipc call brightness decrement 5

# ============ DMS: UI PANELS ============
xkb_rules_layout=us,ru
bind=SUPER,space,switch_keyboard_layout
bind=SUPER,s,spawn,dms ipc call launcher toggle
bind=SUPER,comma,spawn,dms ipc call settings toggle
bind=SUPER,n,switch_layout,next
bind=SUPER,c,spawn,dms ipc call control-center toggle
bind=SUPER,slash,spawn,dms ipc call keybinds toggle
#bind=SUPER,x,toggleoverview
bind=SUPER,x,spawn,dms ipc call hypr toggleOverview


# ============ SCREENSHOT ============
#bind=NONE,Print,spawn,dms ipc call screenshot begin
bind=NONE,Print,spawn,grim -g "$(slurp)" ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png





